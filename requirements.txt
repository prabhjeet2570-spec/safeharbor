# SafeHarbor - proxy-side dependencies
#
# These are the packages needed to run the mitmproxy addon and the PHI
# detection engine (safeharbor_addon.py + phi_redactor.py).
# The FastAPI backend has its own pinned set in backend/requirements.txt.
#
# Install:
#     pip install -r requirements.txt
#     python -m spacy download en_core_web_lg
#
# The spaCy model above is required by Presidio's AnalyzerEngine for
# named-entity recognition (PERSON, LOCATION). Without it, phi_redactor
# falls back to regex-only detection.

mitmproxy>=10.0
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
